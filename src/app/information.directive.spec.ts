import { InformationDirective } from './information.directive';

describe('InformationDirective', () => {
  it('should create an instance', () => {
    const directive = new InformationDirective();
    expect(directive).toBeTruthy();
  });
});
