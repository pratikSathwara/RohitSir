import { LastnameDirective } from './lastname.directive';

describe('LastnameDirective', () => {
  it('should create an instance', () => {
    const directive = new LastnameDirective();
    expect(directive).toBeTruthy();
  });
});
