import { MiddelnameDirective } from './middelname.directive';

describe('MiddelnameDirective', () => {
  it('should create an instance', () => {
    const directive = new MiddelnameDirective();
    expect(directive).toBeTruthy();
  });
});
