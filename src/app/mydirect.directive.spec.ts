import { MydirectDirective } from './mydirect.directive';

describe('MydirectDirective', () => {
  it('should create an instance', () => {
    const directive = new MydirectDirective();
    expect(directive).toBeTruthy();
  });
});
